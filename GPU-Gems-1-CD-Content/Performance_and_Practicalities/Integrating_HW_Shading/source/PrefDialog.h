// from C4Dfx by Jörn Loviscach, www.l7h.cn
// functions used to retrieve values from the preference file

#if !defined(PREFDIALOG_H)
#define PREFDIALOG

bool GetEditorPath(char* s);
bool GetCommandL(char* s, int line, const char* file);

#endif